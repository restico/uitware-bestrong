pr:
  - main

pool:
  name: Self-Hosted

variables:
  - group: bestrong-secrets

jobs:
  - job:
    displayName: Terraform validation
    steps:
      - checkout: self
        displayName: Repository checkout

      - script: echo "Hello, $(mssql-admin-username)!"
        displayName: 'Run a one-line script'